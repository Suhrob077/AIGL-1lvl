{
  "name": "vocab-app",
  "version": "2.0.0",
  "private": true,
  "description": "Vercel deployment root — installs the deps used by api/index.js (the serverless function). Local dev still uses backend/package.json + npm start inside backend/.",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "multer": "^2.0.0",
    "web-push": "^3.6.7",
    "zod": "^3.23.8"
  }
}
